import { createStyles, makeStyles } from '@mui/styles';

export default makeStyles(() => createStyles({
  selectLanguage: {
    '& .MuiSelect-select': {
      padding: '0.313rem 2rem 0.313rem 0.625rem',
    },
  },
}));
